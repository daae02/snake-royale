{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cross-env GH_PAGES=1 vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^7.1.11"
  }
}
